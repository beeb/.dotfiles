{{#if (command_success "test -d $HOME/gitflow")}}
[safe]
    directory = {{env_var "HOME"}}/gitflow
{{/if}}
[user]
    name = {{name}}
    email = {{email}}
    signingkey = {{signkey}}
[core]
    editor = {{#if (is_executable "hx")}}hx{{else}}nano{{/if}}